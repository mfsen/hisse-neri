services:
  bist-signal:
    image: registry.simurgbilisim.com/mfsthissetakip:1
    ports:
      - "5000:5000"
    volumes:
      - ./watchlist.json:/app/watchlist.json
      - ./positions.json:/app/positions.json
    environment:
      - PORT=5000
    restart: unless-stopped
